
/* IMPORT */

//import consolidate from 'consolidate';

//import pify from 'pify';


/* RENDER */

async function render ( contents, file,script ) {
/*
  const render = pify ( consolidate.handlebars.render )
      
    const template = contents.toString (),
          rendered = await render ( template, metadata.renderVariables );

    return new Buffer ( rendered );

  

  
*/
}

/* EXPORT */

export default render;
